They have not understood the problem yet.

Walk them through what I actually did

Logical leap because we are running tests in command line so maybe one would think to access the debugger there?

In the past we saw the test output in the console.

Now we are running the test in the terminal

Where does that leave us for debugging?

Where do we input the breakpoint?

Maybe we put a debugger statement in the code?
Normally we put breakpoints in chrome, but we're in a different place. Try putting debugger statement in code and see if test runs all the way through

Start with 3:05 and just introduce how to use the debugger

How does karma generate the html page?

npm "karma-jasmine-html-reporter


Add in note about absolute folder where the original source files are and don't need to worry about the rest. Don't click the browserify file.

